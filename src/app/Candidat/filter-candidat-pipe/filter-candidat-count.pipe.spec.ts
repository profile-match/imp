/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterCandidatCountPipe } from './filter-candidat-count.pipe';

describe('FilterCandidatCountPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterCandidatCountPipe();
    expect(pipe).toBeTruthy();
  });
});
